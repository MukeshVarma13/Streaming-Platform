import React from "react";

const RightCarosel = ({ title }) => {
  return (
    <div className="bg-theme rounded relative h-full px-5 py-3">
      <h1 className="text-xl">{title}</h1>
    </div>
  );
};

export default RightCarosel;
